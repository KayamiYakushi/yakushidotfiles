[Service]
# Yakushi Qt palette.
# The old hyprpolkitagent package reads SystemPalette, so qt6ct supplies
# a coherent dark application palette including readable light text.
Environment="QT_QPA_PLATFORMTHEME=qt6ct"
Environment="QT_STYLE_OVERRIDE=Fusion"
Environment="QT_QUICK_CONTROLS_STYLE=Fusion"
